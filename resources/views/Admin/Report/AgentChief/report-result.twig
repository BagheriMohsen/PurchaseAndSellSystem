
<!DOCTYPE html>
<html lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>گزارش کلی عملکرد به تفکیک کالا</title>
  <style>
    .invoice_details {
    counter-reset: rowNumber;
    }

    .invoice_details tbody tr {
        counter-increment: rowNumber;
    }

    .invoice_details tbody tr td:first-child::before {
        content: counter(rowNumber);
        min-width: 1em;
        margin-right: 0.5em;
    }
  </style>

  {% include 'Admin.Master.styles' %}
</head>
<body>
  <div class="mt-4 mb-3 text-center col-12 h4">
    {{"گزارش کلی عملکرد به تفکیک کالا"}}
  </div>
<!--End order details modal -->
<div class="row">
  <div class="card col-sm-12">
              
    <!-- /.card-header -->
    <div class="card-body col-12 mx-auto table-responsive">
    
      <table id="searchResult"  class="table table-striped table-bordered table-sm text-center" 
      cellspacing="0"  width="100%"
      >
        <thead>
        <tr>
          <th>نام</th>
          <th>مدیر نماینده</th>
          <th>محصول</th>
          <th>کل سفارشات‌دریافتی</th>
          <th>کل در انتظار تحویل</th>
          <th>کل سفارشات وصولی</th>
          <th>سفارشات معلق</th>
          <th>سفارشات کنسلی</th>
          <th>سفارشات انصراف نهایی</th>
          <th>درصد در انتظار تحویل</th>
          <th>درصد وصول کل</th>
          <th>درصد معلق کل</th>
          <th>درصد کنسلی کل</th>
          <th>درصد انصراف کل</th>
          <th>انبار دریافتی تایید نشده</th>
          <th>موجودی انبار</th>
        </tr>
        </thead>
        <tbody>
          {% for resault in results %}
            <tr>
              <td>{{ resault.agent_name }}</td>
              <td>{{ resault.agentchief_name }}</td>
              <td>{{ resault.product_name }}</td>
              <td>{{ resault.total_order }}</td>
              <td>{{ resault.total_delivary }}</td>
              <td>{{ resault.total_collected }}</td>
              <td>{{ resault.total_suspended }}</td>
              <td>{{ resault.total_cancelled }}</td>
              <td>{{ resault.total_final_cancelled }}</td>
              <td>{{ resault.percent_delivary }}</td>
              <td>{{ resault.percent_collected }}</td>
              <td>{{ resault.percent_suspended }}</td>
              <td>{{ resault.percent_cancelled }}</td>
              <td>{{ resault.percent_final_cancelled }}</td>
              <td>{{ resault.product_onConfirm }}</td>
              <td>{{ resault.product_number }}</td>
            </tr>
          {% endfor %}
      
        </tbody>
        
      </table>
    </div>
    <!-- /.card-body -->
  </div>
</div>



     
</body>
</html>
 {% include 'Admin/Master/scripts' %}